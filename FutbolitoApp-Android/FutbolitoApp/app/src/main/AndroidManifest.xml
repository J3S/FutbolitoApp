<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kevin.futbolitoapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name=".TablaPosicionesActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity-->
            <!--android:name=".TablaPosicionesActivity"-->
            <!--android:label="Tabla de posiciones" />-->
        <activity android:name=".Datos" />
        <activity
            android:name=".JugadoresEquipoActivity"
            android:label="Jugadores" />
        <activity android:name=".EquipoActivity"
            android:label="@string/nom_equipo"></activity>
    </application>

</manifest>